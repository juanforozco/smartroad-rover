\documentclass[12pt]{report}
\usepackage[utf8]{inputenc}
\usepackage[spanish]{babel}
\usepackage{geometry}
\geometry{margin=2.5cm}
\usepackage{graphicx}
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    urlcolor=blue,
    citecolor=black
}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{float}
\usepackage{enumitem}
\usepackage{amssymb}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{amsmath}
\usepackage{listings}
\usepackage{xcolor}

% Estilo de código C
\lstdefinestyle{cstyle}{
    language=C,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue}\bfseries,
    commentstyle=\color{gray}\itshape,
    stringstyle=\color{orange},
    numbers=left,
    numberstyle=\tiny\color{gray},
    breaklines=true,
    frame=single,
    backgroundcolor=\color{gray!5},
    tabsize=4
}

\pagestyle{fancy}
\fancyhf{}
\rhead{SmartRoad Rover}
\lhead{Electrónica Digital III}
\cfoot{\thepage}

\begin{document}

% ============================================================
% PORTADA
% ============================================================
\begin{titlepage}
    \centering
    \vspace*{1cm}
    \includegraphics[width=0.3\textwidth]{Escudo UdeA.jpg}\\[1.5cm]
    {\large \textbf{Electrónica Digital III}}\\[0.5cm]
    {\Huge \textbf{Avance del Proyecto Final}}\\[0.5cm]
    {\LARGE \textbf{SmartRoad Rover}}\\[1cm]
    Juan Felipe Orozco Londoño\\[0.3cm]
    Universidad de Antioquia\\[2cm]
    \today
\end{titlepage}

% ============================================================
% ÍNDICE
% ============================================================
\tableofcontents
\newpage

% ============================================================
% CAPÍTULO 1 — PLAN DE TRABAJO
% ============================================================
\chapter{Plan de Trabajo}

\section{Descripción general}

El proyecto SmartRoad Rover es desarrollado de forma individual por Juan Felipe Orozco Londoño. A continuación se presenta el plan de trabajo estructurado en 6 semanas, con las tareas definidas, su descripción y el estado de avance actual. El lenguaje de programación utilizado para el firmware es \textbf{C}, haciendo uso del \textbf{Pico SDK} oficial de Raspberry Pi, con una arquitectura basada en \textbf{polling e interrupciones (IRQ)}.

\section{Desglose de tareas y avance}

\subsection*{Semana 1 — Definición y diseño del sistema \hfill \textcolor{green!60!black}{100\%}}

\begin{itemize}
    \item Definición de requisitos funcionales y no funcionales.
    \item Selección y justificación de componentes.
    \item Elaboración del diagrama de bloques del hardware.
    \item Estimación del presupuesto.
    \item Configuración del repositorio en GitHub con estructura de carpetas.
\end{itemize}

\subsection*{Semana 2 — Diseño detallado de hardware y arquitectura de software \hfill \textcolor{green!60!black}{100\%}}

\begin{itemize}
    \item Diseño del esquemático del circuito (Pico W, HC-SR04, TB6612FNG, NEO-6M).
    \item Definición de conexiones y asignación de pines del microcontrolador.
    \item Diseño del prototipo sobre protoboard.
    \item Definición de la arquitectura de software en C/Pico SDK: módulos, máquina de estados, flujo de polling e interrupciones.
    \item Documentación del diseño en el repositorio.
\end{itemize}

\subsection*{Semana 3 — Adquisición de componentes y montaje del prototipo \hfill \textcolor{orange}{60\%}}

\begin{itemize}
    \item Adquisición de sensores HC-SR04, driver TB6612FNG, módulo GPS NEO-6M, chasis 2WD, batería 18650 y regulador step-down. \textit{(En proceso)}
    \item Montaje del circuito sobre protoboard. \textit{(Pendiente)}
    \item Verificación de alimentación y continuidad del circuito. \textit{(Pendiente)}
    \item Prueba básica de encendido del sistema. \textit{(Pendiente)}
\end{itemize}

\subsection*{Semana 4 — Desarrollo e integración del firmware \hfill \textcolor{orange!80!red}{15\%}}

\begin{itemize}
    \item Configuración del entorno de desarrollo Pico SDK en C.
    \item Implementación del módulo de lectura de sensores HC-SR04 mediante interrupciones (IRQ en flanco de subida y bajada).
    \item Implementación del módulo de control de motores DC vía PWM.
    \item Implementación del servidor web embebido para control manual vía WiFi.
    \item Implementación de la máquina de estados para gestión de modos de operación.
    \item Integración de módulos y pruebas unitarias.
\end{itemize}

\subsection*{Semana 5 — Integración completa y pruebas del sistema \hfill \textcolor{red}{0\%}}

\begin{itemize}
    \item Integración del firmware completo sobre el prototipo ensamblado.
    \item Pruebas del modo autónomo reactivo (detección, evasión, tiempos de respuesta verificados por UART).
    \item Pruebas del modo manual vía WiFi.
    \item Pruebas del modo GPS en entorno exterior.
    \item Verificación de comportamientos de contingencia (pérdida de WiFi, pérdida de señal GPS).
    \item Corrección de errores y ajustes finos.
\end{itemize}

\subsection*{Semana 6 — Validación final y preparación de la entrega \hfill \textcolor{red}{0\%}}

\begin{itemize}
    \item Pruebas completas en el escenario de pruebas definitivo (entorno exterior, 10 m $\times$ 10 m).
    \item Verificación de todos los requisitos funcionales (RF) y no funcionales (RNF).
    \item Actualización final del repositorio: firmware, hardware, documentación.
    \item Preparación de la sustentación del proyecto.
\end{itemize}

\section{Resumen de avance global}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.4}
\begin{tabular}{|l|c|}
\hline
\textbf{Fase} & \textbf{Avance} \\
\hline
Diseño y planificación del sistema & 100\% \\
Diseño de hardware y esquemático & 100\% \\
Adquisición de componentes y montaje & 60\% \\
Desarrollo de firmware (C / Pico SDK) & 15\% \\
Integración y pruebas & 0\% \\
Validación final y entrega & 0\% \\
\hline
\textbf{Avance general estimado} & \textbf{$\approx$45\%} \\
\hline
\end{tabular}
\caption{Resumen del estado de avance del proyecto}
\end{table}

% ============================================================
% CAPÍTULO 2 — CAMBIOS RESPECTO A LA RONDA 2
% ============================================================
\chapter{Cambios Respecto a la Definición — Ronda 2}

Se describen a continuación todos los cambios aplicados al proyecto respecto a la entrega de la Ronda 2, incluyendo correcciones derivadas de la retroalimentación del docente y decisiones técnicas tomadas durante la fase de diseño detallado.

\section{Lenguaje de programación: MicroPython $\rightarrow$ C / Pico SDK}

El firmware del sistema se implementará en \textbf{C utilizando el Pico SDK oficial} de Raspberry Pi. Esta decisión responde al requerimiento explícito del curso y es técnicamente más adecuada para una arquitectura basada en polling e interrupciones (IRQ), ya que el SDK en C ofrece acceso directo al hardware, control determinista de tiempos y manejo nativo de periféricos como PWM, UART y GPIO sin capas de abstracción intermedias.

\section{Implementación física: PCB $\rightarrow$ Protoboard}

La implementación física del circuito se realizará sobre \textbf{protoboard} en lugar de una placa de circuito impreso (PCB), por las siguientes razones:

\begin{itemize}
    \item El tiempo disponible hasta la entrega final no es suficiente para completar el ciclo de diseño de layout, fabricación, recepción y soldadura de una PCB con garantías de calidad.
    \item La protoboard permite iteraciones rápidas durante el desarrollo y la depuración del firmware, facilitando modificaciones de conexiones sin riesgo de dañar un circuito permanente.
    \item Para un prototipo funcional de validación académica, la protoboard cumple completamente con todos los requisitos funcionales y no funcionales definidos.
\end{itemize}

El esquemático y el presupuesto de fabricación de PCB se conservan como documentación técnica, representando la hoja de ruta para un escalamiento futuro del proyecto. Como complemento, se contempla el diseño de una \textbf{carcasa en impresión 3D} para alojar el conjunto sobre el chasis 2WD, aportando robustez mecánica al prototipo.

\section{Actualización del presupuesto}

Como consecuencia del cambio de PCB a protoboard, el presupuesto se actualiza de la siguiente manera:

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.4}
\begin{tabular}{|l|c|}
\hline
\textbf{Categoría} & \textbf{Subtotal (COP)} \\
\hline
Componentes electrónicos & 126.000 \\
Protoboard y materiales de conexión & 25.000 \\
Plataforma mecánica & 53.000 \\
Sistema de alimentación & 48.000 \\
\hline
\textbf{Total estimado} & \textbf{252.000} \\
\hline
\end{tabular}
\caption{Presupuesto actualizado — prototipo sobre protoboard}
\end{table}

El total se reduce de 312.000 COP a \textbf{252.000 COP}, liberando recursos para la adquisición de componentes faltantes.

\section{Correcciones de la Ronda 2 incorporadas}

Los siguientes ajustes fueron aplicados con base en la retroalimentación del docente:

\begin{itemize}
    \item \textbf{Nota administrativa:} se confirmó que no hubo cambio de nombre del proyecto ni novedades en el equipo de trabajo.
    \item \textbf{RNF reformulados:} el tiempo de respuesta se separó por modo de operación. El modo autónomo reactivo tiene un ciclo determinista menor a 200 ms; el modo manual reconoce que la latencia WiFi no es controlable por el microcontrolador.
    \item \textbf{RNF de tolerancia a fallos agregados:} RNF-5 (detención en $\leq$ 500 ms ante pérdida de WiFi) y RNF-6 (transición a modo autónomo reactivo ante pérdida de señal GPS por más de 3 segundos).
    \item \textbf{Escenario de pruebas unificado:} único entorno exterior de mínimo 10$\times$10 m, materiales de obstáculos especificados (madera o cartón rígido) y verificación de tiempos por timestamps UART en lugar de cronómetro manual.
    \item \textbf{Radio GPS corregido:} reducido de 3 m a 1,5 m para ser coherente con el área de pruebas y la precisión real del módulo NEO-6M.
    \item \textbf{Costo de envío PCB incluido:} partida de envío internacional ($\sim$20.000 COP) incorporada al presupuesto de referencia para escalamiento futuro.
\end{itemize}

% ============================================================
% CAPÍTULO 3 — CIRCUITOS ANALÓGICOS
% ============================================================
\chapter{Circuitos Analógicos Requeridos}

El sistema SmartRoad Rover requiere \textbf{cuatro circuitos analógicos} de acondicionamiento. Tres de ellos son idénticos (un divisor de tensión por cada sensor HC-SR04) y uno corresponde al sistema de alimentación con regulación de voltaje. A continuación se presenta la lista, el procedimiento de diseño de cada uno y su estado de avance.

\section{Lista de circuitos requeridos}

\begin{enumerate}
    \item Divisor de tensión resistivo — sensor HC-SR04 frontal (ECHO: 5 V → 3.3 V)
    \item Divisor de tensión resistivo — sensor HC-SR04 izquierdo (ECHO: 5 V → 3.3 V)
    \item Divisor de tensión resistivo — sensor HC-SR04 derecho (ECHO: 5 V → 3.3 V)
    \item Circuito de alimentación — regulador step-down 7.4 V → 5 V con dos dominios de voltaje
\end{enumerate}

\section{Circuitos 1, 2 y 3 — Divisor de tensión resistivo (HC-SR04 ECHO)}

\subsection{Problema que resuelve}

El Raspberry Pi Pico W opera con lógica de \textbf{3.3 V}. Sus pines GPIO soportan un máximo de 3.3 V en la entrada. El sensor HC-SR04 opera a \textbf{5 V} y devuelve la señal ECHO a nivel de 5 V. Conectar directamente el pin ECHO al GPIO del Pico W daña el microcontrolador de forma permanente. Es necesario adaptar el nivel lógico de 5 V a 3.3 V antes de que la señal llegue al GPIO.

El pin TRIG del HC-SR04 acepta 3.3 V como nivel alto válido, por lo tanto la señal de salida del Pico W va directamente al TRIG sin circuito intermedio.

\subsection{Topología seleccionada}

Divisor resistivo de tensión. Es la solución más simple, económica y confiable para adaptación de nivel lógico unidireccional (el ECHO solo va del sensor al microcontrolador, no al revés). No requiere componentes activos ni encapsulados especiales.

\subsection{Procedimiento de diseño y cálculo}

La ecuación del divisor de tensión es:

\[
V_{out} = V_{in} \cdot \frac{R_2}{R_1 + R_2}
\]

\textbf{Objetivo:} $V_{in} = 5\,\text{V}$, $V_{out} \leq 3.3\,\text{V}$

\textbf{Valores seleccionados:} $R_1 = 1\,\text{k}\Omega$, $R_2 = 2\,\text{k}\Omega$

\textbf{Verificación:}
\[
V_{out} = 5 \cdot \frac{2000}{1000 + 2000} = 5 \cdot \frac{2}{3} = 3.33\,\text{V} \quad \checkmark
\]

Este valor de 3.33 V es seguro para el GPIO del Pico W (máximo 3.3 V, con un margen de tolerancia del 1\%) y suficientemente alto para ser reconocido como nivel lógico HIGH (umbral típico $V_{IH} = 2.0\,\text{V}$).

\textbf{Corriente a través del divisor:}
\[
I = \frac{V_{in}}{R_1 + R_2} = \frac{5}{3000} \approx 1.67\,\text{mA}
\]
Valor despreciable, compatible con la capacidad de corriente del pin ECHO del HC-SR04.

\subsection{Diagrama de conexiones en protoboard}

Cada divisor de tensión se implementa del siguiente modo, replicado tres veces:

\begin{enumerate}
    \item Pin \textbf{VCC} del HC-SR04 → riel de 5 V de la protoboard.
    \item Pin \textbf{GND} del HC-SR04 → riel GND de la protoboard.
    \item Pin \textbf{TRIG} del HC-SR04 → GPIO del Pico W (salida directa, sin circuito intermedio).
    \item Pin \textbf{ECHO} del HC-SR04 → extremo de \textbf{R1 (1 k$\Omega$)}.
    \item Extremo libre de R1 → nodo intermedio en protoboard. Desde este mismo nodo:
    \begin{itemize}
        \item Conectar \textbf{R2 (2 k$\Omega$)} hacia GND.
        \item Conectar un cable hacia el \textbf{pin GPIO del Pico W} (entrada ECHO\_IN).
    \end{itemize}
\end{enumerate}

\subsection{Componentes necesarios por cada divisor (×3)}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{|l|c|c|}
\hline
\textbf{Componente} & \textbf{Valor} & \textbf{Cantidad} \\
\hline
Resistencia & 1 k$\Omega$ / 0.25 W & 1 \\
Resistencia & 2 k$\Omega$ / 0.25 W & 1 \\
Cable jumper macho-macho & — & 4 \\
\hline
\end{tabular}
\caption{Componentes por divisor de tensión (se requieren 3 conjuntos)}
\end{table}

\subsection{Estado de avance}

\textbf{Diseño: 100\%.} Los valores están calculados, verificados y listos para implementar.\\
\textbf{Implementación física: pendiente} (sujeta a la adquisición de los sensores HC-SR04).

\section{Circuito 4 — Sistema de alimentación con regulador step-down}

\subsection{Problema que resuelve}

El sistema tiene \textbf{dos dominios de voltaje}:

\begin{itemize}
    \item \textbf{7.4 V} (batería 18650): requerido directamente por los motores DC a través del pin VM del driver TB6612FNG.
    \item \textbf{5 V regulados}: requeridos por el Pico W (pin VSYS, acepta 1.8--5.5 V), los sensores HC-SR04 (Vcc = 5 V) y el módulo GPS NEO-6M (Vcc = 3.3 V, regulado internamente desde el pin 3V3OUT del Pico W).
\end{itemize}

No es posible alimentar directamente la lógica con 7.4 V sin dañar los componentes. Se requiere un regulador step-down (convertidor buck) que reduzca eficientemente 7.4 V a 5 V.

\subsection{Módulo seleccionado}

Módulo regulador step-down genérico basado en el integrado \textbf{MP1584} o similar. Parámetros relevantes:

\begin{itemize}
    \item Voltaje de entrada: 4.5--28 V (compatible con 7.4 V de la batería).
    \item Voltaje de salida: ajustable; configurado a \textbf{5 V} mediante el potenciómetro del módulo.
    \item Corriente de salida máxima: 3 A.
    \item Eficiencia típica: 92\%.
\end{itemize}

\subsection{Procedimiento de diseño y cálculo de corriente}

Se estima la corriente total del dominio de 5 V:

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{|l|c|}
\hline
\textbf{Componente} & \textbf{Corriente típica} \\
\hline
Raspberry Pi Pico W (WiFi activo) & 100 mA \\
HC-SR04 × 3 & 15 mA × 3 = 45 mA \\
NEO-6M GPS & 45 mA \\
\hline
\textbf{Total dominio 5 V} & \textbf{190 mA} \\
\hline
\end{tabular}
\caption{Consumo estimado del dominio de lógica}
\end{table}

El regulador seleccionado (3 A) entrega un margen de seguridad mayor a 15:1 sobre la carga de lógica. Los motores se alimentan directamente desde la batería a través del TB6612FNG, sin pasar por el regulador.

\subsection{Diagrama de conexiones en protoboard}

\begin{enumerate}
    \item \textbf{Batería (+)} → pin IN+ del módulo step-down \textbf{y} pin VM del TB6612FNG.
    \item \textbf{Batería (-)} → riel GND común de la protoboard.
    \item \textbf{Step-down OUT+} (5 V) → riel de 5 V de la protoboard.
    \item \textbf{Riel 5 V} → pin VSYS del Pico W.
    \item \textbf{Riel 5 V} → pin VCC de cada HC-SR04 (×3).
    \item \textbf{Pin 3V3OUT del Pico W} → pin VCC del módulo GPS NEO-6M.
    \item \textbf{Riel GND común} → GND de todos los componentes del sistema.
\end{enumerate}

\textbf{Nota crítica:} todos los componentes deben compartir el mismo GND (tierra común). Sin tierra común, las señales lógicas no tienen referencia y el sistema no funciona correctamente.

\subsection{Estado de avance}

\textbf{Diseño: 100\%.} El dimensionamiento está completo y el módulo step-down está incluido en el presupuesto.\\
\textbf{Implementación física: pendiente} (sujeta a la adquisición del módulo).

% ============================================================
% CAPÍTULO 4 — DIAGRAMA DE BLOQUES DEL HARDWARE
% ============================================================
\chapter{Diagrama de Bloques del Hardware}

\section{Decisión de diseño: un diagrama jerárquico en tres niveles}

El hardware del sistema SmartRoad Rover integra cinco bloques funcionales (alimentación, sensado, procesamiento, actuación y comunicación), cada uno con señales, niveles de voltaje y protocolos propios. Presentar todo el detalle en un único diagrama produciría un esquema ilegible a escala de página. Por esta razón, y siguiendo la práctica estándar en diseño de sistemas embebidos, se adopta una \textbf{jerarquía de tres diagramas}:

\begin{itemize}
    \item \textbf{Diagrama 1 — Vista general (top-level):} muestra los cinco bloques principales y sus relaciones. No incluye pines ni señales individuales. Es el mapa unificador del sistema.
    \item \textbf{Diagrama 2 — Bloque de sensado y acondicionamiento:} detalla los tres sensores HC-SR04, sus circuitos de adaptación de nivel lógico (divisores de tensión), el módulo GPS y la asignación de pines GPIO del Pico W.
    \item \textbf{Diagrama 3 — Bloque de actuación y alimentación:} detalla el driver TB6612FNG, los dos motores DC, el sistema de alimentación con sus dos dominios de voltaje y la distribución de tierra común.
\end{itemize}

Esta estructura cumple con el requerimiento del enunciado: existe un diagrama que relaciona los demás (Diagrama 1) y cada diagrama de detalle está explicado textualmente en las secciones siguientes.

\section{Asignación de pines GPIO del Pico W}

Antes de describir los diagramas, se fija la asignación definitiva de pines del microcontrolador. Esta tabla es la referencia para el firmware y para el montaje en protoboard:

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{|l|c|c|l|}
\hline
\textbf{Señal} & \textbf{GPIO} & \textbf{Pin físico} & \textbf{Dirección / Nota} \\
\hline
TRIG sensor frontal  & GP0  & 1  & Salida digital \\
ECHO sensor frontal  & GP1  & 2  & Entrada — viene del divisor de tensión \\
TRIG sensor izquierdo & GP2 & 4  & Salida digital \\
ECHO sensor izquierdo & GP3 & 5  & Entrada — viene del divisor de tensión \\
TRIG sensor derecho  & GP4  & 6  & Salida digital \\
ECHO sensor derecho  & GP5  & 7  & Entrada — viene del divisor de tensión \\
GPS TX → Pico RX     & GP9  & 12 & UART1 RX \\
GPS RX → Pico TX     & GP8  & 11 & UART1 TX \\
IN1 Motor A (izq.)   & GP10 & 14 & Salida digital — dirección \\
IN2 Motor A (izq.)   & GP11 & 15 & Salida digital — dirección \\
PWM Motor A (izq.)   & GP12 & 16 & Salida PWM — velocidad \\
IN1 Motor B (der.)   & GP13 & 17 & Salida digital — dirección \\
IN2 Motor B (der.)   & GP14 & 19 & Salida digital — dirección \\
PWM Motor B (der.)   & GP15 & 20 & Salida PWM — velocidad \\
STBY TB6612FNG       & GP16 & 21 & Salida digital — enable global \\
\hline
\end{tabular}
\caption{Asignación definitiva de pines GPIO del Raspberry Pi Pico W}
\end{table}

\section{Diagrama 1 — Vista general del sistema}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.90\linewidth]{diagrama1_toplevel.png}
    \caption{Diagrama de bloques top-level del sistema SmartRoad Rover}
\end{figure}

\textbf{Descripción:} el sistema se organiza alrededor del bloque de procesamiento central (Raspberry Pi Pico W). Este recibe señales del bloque de sensado (sensores ultrasónicos y GPS), genera señales de control hacia el bloque de actuación (driver de motores y motores DC), se comunica bidireccionalmente con el usuario a través del bloque de comunicación WiFi, y recibe energía del bloque de alimentación.

Las relaciones entre bloques son:
\begin{itemize}
    \item \textbf{Alimentación → Procesamiento:} suministra 5 V regulados al pin VSYS del Pico W.
    \item \textbf{Alimentación → Actuación:} suministra 7.4 V directos al pin VM del driver TB6612FNG.
    \item \textbf{Sensado → Procesamiento:} entrega señales digitales de distancia (ECHO adaptado) y tramas NMEA del GPS vía UART.
    \item \textbf{Procesamiento → Actuación:} genera señales PWM de velocidad y señales digitales de dirección hacia el driver.
    \item \textbf{Procesamiento - Comunicación:} intercambio bidireccional vía WiFi 2.4 GHz para recepción de comandos del usuario y envío de estado del sistema.
\end{itemize}

\section{Diagrama 2 — Bloque de sensado y acondicionamiento}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\linewidth]{diagrama2_sensado.png}
    \caption{Diagrama de bloques del bloque de sensado — HC-SR04 ×3, divisores de tensión y módulo GPS}
\end{figure}

\textbf{Descripción:} el diagrama muestra cómo fluyen las señales desde los cuatro sensores hacia el Pico W y qué transformaciones ocurren en el camino. Cada HC-SR04 genera dos señales de distinta naturaleza: una de salida del Pico W (TRIG) que viaja directamente al sensor, y una de entrada (ECHO) que debe pasar por el circuito de acondicionamiento de nivel lógico detallado en el Capítulo 3 antes de llegar al GPIO. Este acondicionamiento es lo que hace el bloque de sensado más complejo que una simple conexión directa y justifica su diagrama propio.

Desde el punto de vista del sistema, lo que el Pico W recibe de este bloque son \textbf{seis señales digitales independientes} (un TRIG de salida y un ECHO de entrada por cada uno de los tres sensores), todas a nivel lógico de 3.3 V, y \textbf{dos señales UART} del GPS (TX y RX). El procesamiento central no necesita conocer los detalles del acondicionamiento; solo ve señales lógicas válidas en sus GPIO.

El GPS introduce un flujo de datos de naturaleza distinta: tramas NMEA seriales a 9600 bps que el Pico W recibe por UART1 y procesa mediante DMA (descrito en el Capítulo 5). Su alimentación proviene del pin 3V3OUT del Pico W, lo que lo hace dependiente del encendido del microcontrolador.

\section{Diagrama 3 — Bloque de actuación y sistema de alimentación}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.90\linewidth]{diagrama3_actuacion.png}
    \caption{Diagrama de bloques del bloque de actuación y sistema de alimentación}
\end{figure}

\textbf{Descripción:} este diagrama muestra dos aspectos del sistema que están íntimamente relacionados: cómo el Pico W controla el movimiento del rover, y cómo la energía de la batería se distribuye a cada componente. Se presentan juntos porque el sistema de alimentación (detallado en el Capítulo 3) determina directamente los voltajes disponibles en cada nodo del bloque de actuación.

Desde el punto de vista del control, el Pico W ejerce autoridad completa sobre el movimiento mediante \textbf{siete señales de salida} hacia el TB6612FNG: tres por canal de motor (dirección A, dirección B y PWM de velocidad) más la señal STBY que habilita o deshabilita globalmente el driver. Esta separación entre señales de dirección (digitales simples) y señal de velocidad (PWM) permite control independiente y preciso de cada rueda, lo que es la base de las maniobras de giro del rover.

Desde el punto de vista energético, el diagrama evidencia la separación de dominios de potencia: los motores operan directamente con la tensión de batería sin regulación, mientras que toda la lógica opera desde el dominio regulado de 5 V. Esta separación protege al microcontrolador de los picos de corriente que generan los motores al arrancar o frenar bruscamente.

% ============================================================
% CAPÍTULO 5 — ARQUITECTURA DE SOFTWARE
% ============================================================
\chapter{Arquitectura de Software}

\section{Visión general y decisiones de diseño}

\textbf{Nota sobre el estado del firmware:} el código presentado en este capítulo constituye la \textbf{arquitectura de firmware diseñada}. Los fragmentos mostrados representan la implementación planificada de cada módulo, resultado de las decisiones de diseño tomadas durante las Semanas 1 y 2 del plan de trabajo. La compilación, integración y prueba sobre el hardware físico corresponden a las Semanas 4 y 5, cuyo avance actual se describe en el Capítulo 1.

El firmware se implementa en \textbf{C con el Pico SDK oficial} de Raspberry Pi. La arquitectura adopta el paradigma \textbf{Polling + IRQ}: las operaciones de tiempo crítico (medición de eco ultrasónico, recepción GPS) se manejan mediante interrupciones de hardware; la lógica de navegación, la gestión de modos y el servidor web operan mediante polling en el loop principal. No se usa RTOS. Los periféricos del RP2040 (GPIO IRQ, PWM hardware, UART, DMA, timers de hardware) se explotan directamente para lograr comportamiento determinista sin bloquear el CPU.

\section{Estructura modular del firmware}

El código se organiza en módulos independientes, cada uno con su archivo \texttt{.c} y su cabecera \texttt{.h}:

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{0.85}
\begin{tabular}{|l|p{8.5cm}|}
\hline
\textbf{Módulo} & \textbf{Responsabilidad} \\
\hline
\texttt{main.c} & Loop principal, inicialización del sistema, despacho de la FSM \\
\hline
\texttt{sensors.c/.h} & Disparo TRIG (polling), ISR ECHO, cálculo de distancia, filtrado por mediana \\
\hline
\texttt{motors.c/.h} & PWM de hardware, control de dirección, abstracción del TB6612FNG \\
\hline
\texttt{navigation.c/.h} & Algoritmo de evasión autónoma, selección de dirección de giro \\
\hline
\texttt{web\_server.c/.h} & Servidor HTTP embebido (lwIP), parsing de comandos, watchdog WiFi \\
\hline
\texttt{gps.c/.h} & UART1 + DMA, parsing de tramas NMEA, cálculo de rumbo, watchdog GPS \\
\hline
\texttt{fsm.c/.h} & FSM principal: 5 estados, transiciones nominales y de contingencia \\
\hline
\end{tabular}
\caption{Estructura modular del firmware SmartRoad Rover}
\end{table}

Cada módulo es compilable y testeable de forma independiente. La variable de estado de la FSM es \texttt{volatile} para garantizar visibilidad entre el contexto de IRQ y el loop principal.

\section{Máquina de estados finita (FSM) — \texttt{fsm.c}}

La FSM principal tiene \textbf{5 estados}. Solo un estado está activo en cualquier instante. Se implementa con \texttt{enum} y \texttt{switch-case} en el loop principal:

\begin{lstlisting}[style=cstyle]
typedef enum {
    STATE_INIT,
    STATE_MANUAL,
    STATE_AUTO_REACTIVE,
    STATE_AUTO_GPS,
    STATE_SAFE_STOP
} system_state_t;

volatile system_state_t current_state = STATE_INIT;

int main(void) {
    system_init();
    while (true) {
        switch (current_state) {
            case STATE_INIT:          fsm_init();          break;
            case STATE_MANUAL:        fsm_manual();        break;
            case STATE_AUTO_REACTIVE: fsm_auto_reactive(); break;
            case STATE_AUTO_GPS:      fsm_auto_gps();      break;
            case STATE_SAFE_STOP:     fsm_safe_stop();     break;
        }
    }
}
\end{lstlisting}

\textbf{Transiciones nominales:} INIT $\rightarrow$ MANUAL / AUTO\_REACTIVE / AUTO\_GPS según comando del usuario vía web. Cualquier estado $\leftrightarrow$ cualquier otro por comando web.

\textbf{Transiciones de contingencia:}
\begin{itemize}
    \item Pérdida de WiFi $>$ 500 ms $\rightarrow$ \texttt{STATE\_SAFE\_STOP} (RNF-5)
    \item Pérdida de señal GPS $>$ 3 s $\rightarrow$ \texttt{STATE\_SAFE\_STOP} $\rightarrow$ \texttt{STATE\_AUTO\_REACTIVE} (RNF-6)
    \item Reconexión o comando de usuario $\rightarrow$ salida de \texttt{STATE\_SAFE\_STOP}
\end{itemize}

La figura~\ref{fig:fsm} ilustra el diagrama de transiciones completo de la FSM principal. Las flechas indican las condiciones que disparan cada cambio de estado, incluyendo los casos de contingencia: pérdida de conexión WiFi en modo manual y pérdida de señal GPS en modo de navegación, ambos con transición automática hacia \texttt{STATE\_SAFE\_STOP}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.90\linewidth]{fsm_principal.pdf}
    \caption{Diagrama FSM principal del sistema SmartRoad Rover — Realizado con NotebookLM}
    \label{fig:fsm}
\end{figure}

\section{Módulo de sensores — ISR y filtrado (\texttt{sensors.c})}

La medición de distancia con HC-SR04 exige medir el tiempo de vuelo del pulso ECHO con precisión de microsegundos. Se usan \textbf{interrupciones GPIO en ambos flancos}:

\begin{lstlisting}[style=cstyle]
void echo_irq_handler(uint gpio, uint32_t events) {
    uint32_t now = time_us_32();
    uint8_t idx = gpio_to_sensor_index(gpio);

    if (events & GPIO_IRQ_EDGE_RISE) {
        sensor_data[idx].t_rise = now;
    }
    if (events & GPIO_IRQ_EDGE_FALL) {
        uint32_t pulse_us = now - sensor_data[idx].t_rise;
        sensor_data[idx].distance_cm = (pulse_us * 17) / 1000;
        sensor_data[idx].valid = true;
    }
}
\end{lstlisting}

\textbf{Filtrado por mediana (RNF-9):} cada canal mantiene un buffer circular de 3 lecturas. La distancia reportada al resto del sistema es la mediana de las últimas 3 mediciones válidas, descartando picos por reflexiones espurias sin introducir latencia significativa.

\textbf{Disparo de TRIG (polling):} síncrono desde el loop principal. Se usa \texttt{busy\_wait\_us(10)} del SDK, determinista y sin bloquear el sistema de interrupciones:

\begin{lstlisting}[style=cstyle]
void sensors_trigger_all(void) {
    const uint trig_pins[] = {GP0, GP2, GP4};
    for (int i = 0; i < NUM_SENSORS; i++) {
        gpio_put(trig_pins[i], 1);
        busy_wait_us(10);
        gpio_put(trig_pins[i], 0);
    }
}
\end{lstlisting}
\newpage
\section{Módulo de motores — PWM de hardware (\texttt{motors.c})}

Se usa \textbf{PWM de hardware} del RP2040 a 20 kHz (inaudible, adecuado para motores DC pequeños). Cada motor tiene un slice PWM independiente:

\begin{lstlisting}[style=cstyle]
void motors_init(void) {
    gpio_set_function(GP12, GPIO_FUNC_PWM);  /* Motor A */
    gpio_set_function(GP15, GPIO_FUNC_PWM);  /* Motor B */
    uint slice_a = pwm_gpio_to_slice_num(GP12);
    uint slice_b = pwm_gpio_to_slice_num(GP15);
    pwm_config cfg = pwm_get_default_config();
    pwm_config_set_clkdiv(&cfg, 6.25f);  /* 20 kHz */
    pwm_config_set_wrap(&cfg, 999);       /* resolucion 0-999 */
    pwm_init(slice_a, &cfg, true);
    pwm_init(slice_b, &cfg, true);
}

void motors_set(motor_dir_t dir_a, uint16_t spd_a,
                motor_dir_t dir_b, uint16_t spd_b) {
    gpio_put(GP10, (dir_a == FORWARD) ? 1 : 0);
    gpio_put(GP11, (dir_a == FORWARD) ? 0 : 1);
    gpio_put(GP13, (dir_b == FORWARD) ? 1 : 0);
    gpio_put(GP14, (dir_b == FORWARD) ? 0 : 1);
    pwm_set_gpio_level(GP12, spd_a);
    pwm_set_gpio_level(GP15, spd_b);
}
\end{lstlisting}
\newpage
\section{Módulo de navegación autónoma (\texttt{navigation.c})}

Implementa el algoritmo de evasión. Se ejecuta en el loop principal consumiendo datos ya calculados por la ISR, sin bloquear el sistema de interrupciones.

\textbf{Caso de contingencia: los tres sensores detectan obstáculo simultáneamente.} Si los tres sensores reportan distancia menor al umbral, el rover ejecuta una maniobra de retroceso durante 400 ms seguida de un giro a la derecha por defecto durante 300 ms, antes de retomar el ciclo. Esta decisión es determinista y garantiza que el sistema nunca quede bloqueado indefinidamente:

\begin{lstlisting}[style=cstyle]
void navigation_step(void) {
    uint16_t front = sensors_get_filtered(FRONT);
    uint16_t left  = sensors_get_filtered(LEFT);
    uint16_t right = sensors_get_filtered(RIGHT);

    /* Caso especial: los tres sensores detectan obstaculo */
    if (front < OBSTACLE_THRESHOLD_CM &&
        left  < OBSTACLE_THRESHOLD_CM &&
        right < OBSTACLE_THRESHOLD_CM) {
        motors_set(REVERSE, SPEED_TURN, REVERSE, SPEED_TURN);
        busy_wait_ms(400);
        motors_set(FORWARD, SPEED_TURN, REVERSE, SPEED_TURN);
        busy_wait_ms(300);
        return;
    }

    if (front > OBSTACLE_THRESHOLD_CM) {
        motors_set(FORWARD, SPEED_FULL, FORWARD, SPEED_FULL);
    } else {
        motors_set(STOP, 0, STOP, 0);
        busy_wait_ms(50);
        if (left >= right) {
            motors_set(REVERSE, SPEED_TURN, FORWARD, SPEED_TURN);
        } else {
            motors_set(FORWARD, SPEED_TURN, REVERSE, SPEED_TURN);
        }
        busy_wait_ms(300);
    }
}
\end{lstlisting}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.73\linewidth]{flowchart_auto_reactive.pdf}
    \caption{Diagrama de flujo — modo autónomo reactivo (Polling + IRQ) — Realizado con NotebookLM}
    \label{fig:flowchart}
\end{figure}
\newpage
\section{Módulo GPS con DMA (\texttt{gps.c}) — Reporte DMA}

Se usa \textbf{1 canal DMA} para recepción de tramas NMEA por UART1. El DMA transfiere bytes del registro \texttt{UART1\_DR} a un buffer circular en RAM mediante el DREQ \texttt{DREQ\_UART1\_RX}, sin intervención del CPU. Cuando la transferencia completa, \texttt{DMA\_IRQ\_0} señaliza al loop principal que hay una trama lista:

\begin{lstlisting}[style=cstyle]
void gps_init(void) {
    uart_init(UART_GPS, GPS_BAUD);
    gpio_set_function(GP8, GPIO_FUNC_UART);  /* TX */
    gpio_set_function(GP9, GPIO_FUNC_UART);  /* RX */

    dma_channel = dma_claim_unused_channel(true);
    dma_channel_config cfg =
        dma_channel_get_default_config(dma_channel);
    channel_config_set_transfer_data_size(&cfg, DMA_SIZE_8);
    channel_config_set_read_increment(&cfg, false);
    channel_config_set_write_increment(&cfg, true);
    channel_config_set_dreq(&cfg, DREQ_UART1_RX);

    dma_channel_configure(dma_channel, &cfg,
        gps_buffer,
        &uart_get_hw(UART_GPS)->dr,
        GPS_BUF_SIZE, true);

    dma_channel_set_irq0_enabled(dma_channel, true);
    irq_set_exclusive_handler(DMA_IRQ_0, gps_dma_irq_handler);
    irq_set_enabled(DMA_IRQ_0, true);
}
\end{lstlisting}

\textbf{Watchdog GPS (RNF-6):} un repeating timer de 1 s verifica si han transcurrido más de 3 segundos sin trama válida. De ser así, transita a \texttt{STATE\_SAFE\_STOP}:

\begin{lstlisting}[style=cstyle]
bool gps_signal_valid(void) {
    return (time_us_64() - gps_last_valid_us) < GPS_TIMEOUT_US;
}
\end{lstlisting}
\newpage
\section{Módulo servidor web — Watchdog WiFi (\texttt{web\_server.c})}

Servidor HTTP embebido sobre el stack \textbf{lwIP} del Pico W SDK. El parsing de comandos es no bloqueante mediante callbacks de lwIP. Un repeating timer de 100 ms verifica el timeout del último comando recibido (RNF-5):

\begin{lstlisting}[style=cstyle]
bool wifi_watchdog_callback(repeating_timer_t *t) {
    if ((time_us_64() - last_cmd_us) > WIFI_TIMEOUT_US) {
        motors_set(STOP, 0, STOP, 0);
        current_state = STATE_SAFE_STOP;
    }
    return true;
}
\end{lstlisting}

\section{Inventario de interrupciones}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.4}
\begin{tabular}{|l|l|p{5.5cm}|}
\hline
\textbf{Fuente IRQ} & \textbf{Tipo} & \textbf{Acción en el handler} \\
\hline
GPIO GP1 — ECHO frontal & Flanco $\uparrow\downarrow$ & Captura tiempo, calcula distancia, actualiza buffer \\
\hline
GPIO GP3 — ECHO izquierdo & Flanco $\uparrow\downarrow$ & Ídem \\
\hline
GPIO GP5 — ECHO derecho & Flanco $\uparrow\downarrow$ & Ídem \\
\hline
DMA\_IRQ\_0 & Transferencia completa & Señaliza trama GPS disponible, recarga DMA \\
\hline
Repeating timer 100 ms & Periódica & Watchdog WiFi — verifica timeout HTTP \\
\hline
Repeating timer 1 s & Periódica & Watchdog GPS — verifica timeout señal \\
\hline
\end{tabular}
\caption{Inventario de interrupciones del firmware}
\end{table}

\section{Librerías del Pico SDK utilizadas}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{|l|p{8cm}|}
\hline
\textbf{Librería} & \textbf{Uso} \\
\hline
\texttt{pico/stdlib.h} & Funciones base, stdio, delays \\
\hline
\texttt{hardware/gpio.h} & Configuración de pines e IRQ GPIO \\
\hline
\texttt{hardware/pwm.h} & PWM de hardware para motores \\
\hline
\texttt{hardware/uart.h} & Comunicación UART con GPS \\
\hline
\texttt{hardware/dma.h} & Canal DMA para recepción GPS \\
\hline
\texttt{hardware/timer.h} & \texttt{time\_us\_32/64()}, repeating timers \\
\hline
\texttt{pico/cyw43\_arch.h} & WiFi del chip CYW43439 del Pico W \\
\hline
\texttt{lwip/tcp.h} & Stack TCP/IP para servidor HTTP embebido \\
\hline
\end{tabular}
\caption{Librerías del Pico SDK empleadas en el firmware}
\end{table}

% ============================================================
% CAPÍTULO 6 — MANEJO DEL REPOSITORIO
% ============================================================
\chapter{Manejo del Repositorio}
 
\section{Repositorio del proyecto}
 
El repositorio del proyecto SmartRoad Rover se encuentra disponible en:
 
\begin{center}
\url{https://github.com/juanforozco/smartroad-rover}
\end{center}
 
El repositorio ha estado activo desde el inicio del proceso de definición del proyecto y ha sido actualizado de forma continua a lo largo de cada entregable, reflejando en todo momento el estado real del avance.
 
\section{Estructura de carpetas}
 
El repositorio está organizado en cuatro carpetas principales, cada una con su propio \texttt{README.md}:
 
\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.4}
\begin{tabular}{|l|p{9cm}|}
\hline
\textbf{Carpeta} & \textbf{Contenido} \\
\hline
\texttt{docs/} & Documentos formales de entrega organizados por entregable en subcarpetas (\texttt{entregable1/}, \texttt{entregable2/}, \texttt{entregable3/}). Cada subcarpeta contiene el archivo fuente LaTeX y los PDFs generados. \\
\hline
\texttt{firmware/} & Código fuente del microcontrolador en C con el Pico SDK. Contendrá los archivos \texttt{.c} y \texttt{.h} de cada módulo (\texttt{sensors}, \texttt{motors}, \texttt{navigation}, \texttt{web\_server}, \texttt{gps}, \texttt{fsm}) junto con el \texttt{CMakeLists.txt}. \\
\hline
\texttt{hardware/} & Documentación del hardware: esquemático del circuito y tabla de asignación de pines del Pico W. \\
\hline
\texttt{images/} & Imágenes de la documentación: diagramas de bloques, FSM, diagramas de flujo y fotos del prototipo. \\
\hline
\end{tabular}
\caption{Estructura de carpetas del repositorio}
\end{table}
 
\section{Estrategia de ramas}
 
El repositorio trabaja con dos ramas principales:
 
\begin{itemize}
    \item \textbf{main:} rama de producción. Solo recibe commits cuando un entregable o funcionalidad está completamente terminada y verificada. Refleja el estado oficial y estable del proyecto en todo momento.
    \item \textbf{dev:} rama de desarrollo activo. Aquí se trabajan los cambios, se agregan documentos en progreso y se desarrolla el firmware antes de integrarlo a \texttt{main}. Una vez verificado, se realiza un merge a \texttt{main} mediante pull request.
\end{itemize}
 
Esta estrategia garantiza que \texttt{main} siempre tenga un estado coherente y presentable, mientras que \texttt{dev} refleja el trabajo en curso sin afectar la versión oficial.
 
\section{Convención de mensajes de commit}
 
Se usa una convención de prefijos para que el historial sea legible y trazable:
 
\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{|l|p{9cm}|}
\hline
\textbf{Prefijo} & \textbf{Uso} \\
\hline
\texttt{docs:} & Cambios en documentación y entregables \\
\texttt{firmware:} & Código del microcontrolador \\
\texttt{hardware:} & Esquemáticos y asignación de pines \\
\texttt{fix:} & Correcciones de cualquier tipo \\
\texttt{refactor:} & Reorganización sin cambio de funcionalidad \\
\hline
\end{tabular}
\caption{Convención de mensajes de commit}
\end{table}
 
Ejemplos del historial actual: \textit{"docs: agregar capítulo 5 - arquitectura de software"}, \textit{"docs: correcciones ronda 2 basadas en retroalimentación docente"}, \textit{"docs: crear estructura entregable 3"}.
 
\section{Integración con VS Code y Pico SDK}
 
Durante la fase de implementación del firmware, el flujo de trabajo será el siguiente: el repositorio se clona localmente, VS Code abre directamente la carpeta del repositorio como espacio de trabajo, y la extensión oficial de Raspberry Pi Pico se configura apuntando a esa misma carpeta. Esto permite editar el código, compilar con el Pico SDK y realizar commits y push a GitHub, todo desde VS Code sin necesidad de alternar entre herramientas. El archivo \texttt{CMakeLists.txt} del proyecto estará en \texttt{firmware/} y cada módulo tendrá su propio archivo fuente, coherente con la arquitectura modular descrita en el Capítulo 5.
 
\section{Estado actual del repositorio}
 
El repositorio refleja el avance real del proyecto en cada una de sus secciones:
 
\begin{itemize}
    \item \texttt{docs/}: documentos de los tres entregables subidos y organizados por subcarpeta.
    \item \texttt{images/}: diagramas de bloques (top-level, sensado, actuación), FSM principal y diagrama de flujo del modo autónomo reactivo.
    \item \texttt{firmware/}: carpeta preparada con su \texttt{README.md} describiendo la estructura modular planificada, lista para recibir el código de implementación.
    \item \texttt{hardware/}: esquemático y tabla de pines documentados.
    \item \texttt{README.md} principal: actualizado con descripción completa del sistema, modos de operación, requisitos, escenario de pruebas y presupuesto vigente (252.000 COP).
\end{itemize}

% ============================================================
% REFERENCIAS
% ============================================================
\begin{thebibliography}{9}

\bibitem{picosdk}
Raspberry Pi Foundation. \textit{Raspberry Pi Pico C/C++ SDK}. \url{https://datasheets.raspberrypi.com/pico/raspberry-pi-pico-c-sdk.pdf}

\bibitem{picow}
Raspberry Pi Foundation. \textit{Raspberry Pi Pico W Datasheet}.

\bibitem{hcsr04}
\textit{HC-SR04 Ultrasonic Sensor Datasheet}.

\bibitem{tb6612}
Toshiba. \textit{TB6612FNG Motor Driver Datasheet}.

\bibitem{neo6m}
u-blox. \textit{NEO-6M GPS Module Datasheet}.

\bibitem{barr}
Michael Barr. \textit{Embedded Systems Engineering}. O'Reilly.

\end{thebibliography}

\end{document}
